# 第9课：模拟法

## 学习目标

掌握状态、操作、终止；完成得分、坐标、方向和多状态模拟；用状态表调试；完成12道客观题与6道编程题。

## 模拟三要素

状态是当前关键信息，操作描述一次输入如何改变状态，终止条件决定何时停止。模拟不是猜结果，而是忠实执行规则。

## 得分与坐标

得分通常更新后判断结束。坐标题先确认坐标约定；方向数组统一表示上下左右。先算候选位置，合法后才移动。

## 多状态

需要旧状态时使用临时变量或新数组，避免原地修改污染。每轮检查不变量。

## 调试

用状态表逐轮记录，重点测试初始即结束、最后一步、越界、并列和全零。
